services:
  - type: web
    name: attendence-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir api send_sms:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
